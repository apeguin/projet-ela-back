[core]
	engine = pg
	top_dir = migrations
	# plan_file = migrations/sqitch.plan
# [engine "pg"]
	target = db:pg://loup_argent:loup_argent@localhost/loup_argent_versioned
    # target = db:pg:loup_argent_versioned
	# registry = sqitch
	# client = psql
[deploy]
	verify = true
